---
interface Props {
  bgClass?: string;
}

const { bgClass = '' } = Astro.props;
---

<div class={`section min-h-0 min-w-0 max-w-full ${bgClass}`.trim()}>
  <div class="container-default w-container min-h-0 min-w-0 max-w-full">
    <slot />
  </div>
</div>
